PORT=4000
NODE_ENV=development

# Postgres connection — fill in your local password
DATABASE_URL= "postgresql://neondb_owner:npg_og5ml7znLDPq@ep-late-night-anta4fd4-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Generate a long random string for production
JWT_SECRET= QWERTYUIOPASDFGHJKLZXCVBNM1234567890abcdefABCDEFGH
JWT_EXPIRES_IN=7d

# Comma-separated list of allowed origins for CORS
CORS_ORIGIN=http://localhost:5173

# Single plaintext password used for all seeded users (dev only)
SEED_PASSWORD=password123
